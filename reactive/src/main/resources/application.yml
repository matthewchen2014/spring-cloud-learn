spring:
  data:
    mongodb:
      database: test
      host: localhost
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: SA
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

